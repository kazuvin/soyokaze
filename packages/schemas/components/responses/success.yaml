UserResponse:
  description: User data
  content:
    application/json:
      schema:
        $ref: '../schemas/user.yaml'

UserCreated:
  description: User created successfully
  content:
    application/json:
      schema:
        $ref: '../schemas/user.yaml'

UserUpdated:
  description: User updated successfully
  content:
    application/json:
      schema:
        $ref: '../schemas/user.yaml'

UserDeleted:
  description: User deleted successfully

UsersListResponse:
  description: List of users
  content:
    application/json:
      schema:
        $ref: '../schemas/common.yaml#/UsersListResponse'